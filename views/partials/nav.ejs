<header>
    <ul>
        <h2><%= squad.cohort %></h2>
        <% squads.forEach((squad) => { %>
        <li>
            <a href="/?id=<%= squad.id %>"><%= squad.name %></a>
        </li>
        <% }) %>
    </ul>
</header>

